---
/**
 * Wrap answer markdown for use in Question's answer slot.
 * MDX only parses markdown in a component's default slot, so use:
 *
 * <QuestionAnswer slot="answer">…markdown…</QuestionAnswer>
 */
---

<slot />
